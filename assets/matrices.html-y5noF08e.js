import{_ as i,c as r,a as t,d as a,e as s,w as n,b as c,r as d,o as l}from"./app-Dh050PCp.js";const h={};function m(u,e){const o=d("RouteLink");return l(),r("div",null,[e[3]||(e[3]=t("h1",{id:"matrices",tabindex:"-1"},[t("a",{class:"header-anchor",href:"#matrices"},[t("span",null,"Matrices")])],-1)),e[4]||(e[4]=t("p",null,[t("em",null,"Build sophisticated matrices in TW. Use them to create keys, score images and observations, manage projects, and streamline creation of custom outputs and sharing of descriptors.")],-1)),e[5]||(e[5]=t("h2",{id:"nomenclature-quick-start",tabindex:"-1"},[t("a",{class:"header-anchor",href:"#nomenclature-quick-start"},[t("span",null,"Nomenclature Quick Start")])],-1)),t("p",null,[e[1]||(e[1]=a("TW provides a suite of common tasks help you to manage your matrices, descriptors, observations and related data. This section assumes you have some familiarity with the ",-1)),s(o,{to:"/guide/Manual/conventions.html"},{default:n(()=>[...e[0]||(e[0]=[a("TW user interface conventions",-1)])]),_:1}),e[2]||(e[2]=a(" and introduces new user interface features where needed. Next, please familiarize yourself with the kinds of tasks TW offers related to Matrices.",-1))]),e[6]||(e[6]=c('<h2 id="understanding-the-data-model" tabindex="-1"><a class="header-anchor" href="#understanding-the-data-model"><span>Understanding the data model</span></a></h2><p>Extensive information exists about the <a href="https://docs.taxonworks.org/develop/Data/models.html" target="_blank" rel="noopener noreferrer">TaxonWorks data model</a>. Information is presented in text and searchable graphic formats. A few comments:</p><ul><li>Each matrix consistas of defined <code>Rows</code> and <code>Columns</code>.</li><li><code>Rows</code> are <code>OTUs</code>, <code>Collection Objects</code> and other objects in the database which could be added to a matrix. The same OTU or any other object could be added to as many matrices as needed.</li><li><code>Columns</code> are <code>Descriptors</code> (characters) which could be added to a matrix. The same descriptor could be added to sevaral matrices.</li><li>TaxonWorks does not store the entire matrix, instead we store individual observations for each cell in the table.</li></ul><div class="hint-container important"><p class="hint-container-title">Important</p><p>In practice this means that if you have two matrices which include the same OTU and the same descriptor, the observations in that OTU/descriptor cell will be identical in both matrices, regardless of which matrix the original observation was made in.</p></div><p>This means you can clone and combine matrices as needed: observations will be replicated for each matrix having the same combination of Row object and Column object.</p><figure><img src="https://sfg.taxonworks.org/s/s8pmvv" alt="" title=""><figcaption class="figure-left">A portion of a TaxonWorks Observation Matrix</figcaption></figure><h2 id="curating-data" tabindex="-1"><a class="header-anchor" href="#curating-data"><span>Curating data</span></a></h2><h3 id="basic-information" tabindex="-1"><a class="header-anchor" href="#basic-information"><span>Basic information</span></a></h3><ul><li>In most cases, work with matrices starts in the <code>Observation Matrix Hub</code> task.</li><li>Click the <code>New</code> button to create a new matrix. <ul><li>After clicking <code>New</code> you can give your matrix a name and optionally an OTU which the matrix describes. Check the <code>Is public</code> checkbox if you&#39;d like the interactive key based on your matrix to appear in Taxon Pages, or if you&#39;d like your matrix to be accessible from the API. Now click <code>Create</code>: you&#39;re now in <code>Edit</code> mode, which we&#39;ll cover below.</li></ul></li><li>Alternatively, in the <code>Observation Matrix Hub</code>, find an existing matrix from the list and choose <code>Edit</code> from its radial navigator.</li><li>You can also pin a matrix in the <code>Hub</code>, so that it can be used as a default matrix in various interfaces.</li></ul><p>Once in <code>Edit</code> mode, rows and columns need to be defined.</p><h3 id="adding-a-descriptor-column-to-the-matrix" tabindex="-1"><a class="header-anchor" href="#adding-a-descriptor-column-to-the-matrix"><span>Adding a Descriptor (column) to the Matrix</span></a></h3><ul><li>To add a new <code>Descriptor</code>, click the <code>New Descriptor</code> link at the top of the interface. Select the descriptor <code>Type</code>. The available types are: <ul><li><code>Qualitative</code> - traditional phylogenetic descriptor with multiple character states</li><li><code>Presence/absence</code> - similar to the previous one, but only two predefined states are possible</li><li><code>Quantitative</code> - a single measurement character (with an optional measurement unit)</li><li><code>Sample</code> - a numeric character, for which a min/max range and other statistical data can be scored (with optional measurement units per observation)</li><li><code>Gene</code> - ???</li><li><code>Free text</code></li><li><code>Media</code> - a specialized descriptor designed for attachment of <code>Depictions</code>, which serve as a visual observation in image matrices (see <a href="#image-matrices">below</a> for details)</li></ul></li><li>Show more mode allows adding some additional details about the descriptor: <code>Short name</code>, <code>Description name</code> - used for autogenerated descriptions, <code>Key name</code> - used in the interactive key of the matrix, <code>Weight</code> - larger values bring the descriptor closer to the top in an interactive key.</li><li>If <code>Qualitative Descriptor</code> is selected, two or more character states need to be defined. Each <code>Character state</code> has two fields: <code>Label</code> (&#39;0&#39;, &#39;1&#39;, &#39;2&#39;, ...) and <code>Name</code>. Some additional information can be provided when <code>Show more</code> is selected. <ul><li>An example of a Qualitative descriptor: &#39;Body color&#39; with two states: &#39;0&#39; - red, &#39;1&#39; - black.</li></ul></li></ul><figure><img src="https://sfg.taxonworks.org/s/k8txsj" alt="" title=""><figcaption class="figure-left">Example of creating a Qualitative Descriptor; the <code>red</code> state has already been added</figcaption></figure><ul><li>Once the descriptor information is entered you can click one of the <code>Create</code> buttons. In all cases, the newly created descriptor is added to the matrix as a <code>Column</code>. <ul><li><code>Create</code> will save the record, but you can continue editing the descriptor</li><li><code>Create and return to matrix</code> will return you back to the Edit matrix interface</li><li><code>Create and new</code> can be used if multiple descriptors need to be defined</li></ul></li><li>Alternatively, if descriptors already exist in the database, they can be added to an existing matrix in the <code>Edit observation matrix</code> interface by using the search interface on the lower left side. Change the <code>Row\\Column</code> switch to <code>Column</code>, the <code>Dynamic\\Fixed</code> switch to <code>Fixed</code>, and use the <code>Search</code> option. Alternatively, multiple descriptors from another matrix can be added using the <code>From Another Matrix</code> option. As explained above, neither of these options creates a new descriptor, it simply associates an existing descriptor with this matrix as a column.</li></ul><figure><img src="https://sfg.taxonworks.org/s/a57jqb" alt="" title=""><figcaption class="figure-left">A matrix to which 4 descriptors have been added (the right column)</figcaption></figure><h3 id="adding-an-otu-or-collection-object-row-to-the-matrix" tabindex="-1"><a class="header-anchor" href="#adding-an-otu-or-collection-object-row-to-the-matrix"><span>Adding an OTU or Collection Object (row) to the Matrix</span></a></h3><ul><li>To add <code>Rows</code> to the matrix, change the <code>Row/Column</code> switch to <code>Row</code> and change the <code>Dynamic/Fixed</code> switch to the <code>Fixed</code> position. <code>OTU</code> and <code>Collection Object</code> are the mostly frequently used types as <code>Rows</code>, but other types can be added as well; select the <code>Search</code> tab to see your options.</li></ul><figure><img src="https://sfg.taxonworks.org/s/615lc0" alt="" title=""><figcaption class="figure-left">Search options for observation matrix <code>Rows</code> in the lower left; multiple rows have already been added to this matrix</figcaption></figure><p>Alternatively you can add rows from your pinboard, by selecting from tagged objects, or from another matrix.</p><p>Other options for adding <code>Rows</code>:</p><ul><li>You can add a new <code>Row</code> to the matrix directly from the <code>Browse OTU</code> or <code>Browse Nomenclature</code> task. In either task, select the <code>OTU radial</code> button, then the <code>Observation matrices</code> sector; from there you can select a matrix to automatically add this OTU to it and proceed directly to scoring mode (discussed below).</li><li>You can add many <code>Rows</code> at a time by selecting from filter results in any filter that has the <code>Radial matrix</code> button <img src="https://sfg.taxonworks.org/s/286w1x" width="20px">, including <code>Filter OTU</code>, <code>Filter Collection Object</code>, etc.</li><li>An Advanced option to add multiple OTUs to a matrix is the use of <code>Dynamic</code> rows. This option allows you to select either all OTUs marked with a specific <code>Tag</code> or select a parent taxon, for example a genus, to add all children taxa, for example species. Such rows are dynamic in the sense that if a new OTU is tagged or untagged with your chosen tag, or if new species are added to or deleted from your selected genus in the future, they will automatically be added to or removed from the matrix.</li></ul><h3 id="reordering-and-editing-rows-and-columns-in-the-matrix" tabindex="-1"><a class="header-anchor" href="#reordering-and-editing-rows-and-columns-in-the-matrix"><span>Reordering And Editing Rows and Columns in the Matrix</span></a></h3><ul><li>With the <code>Sortable columns/rows</code> checkbox at the top checked, you can resort columns and rows by dragging each up or down the list.</li><li>To delete a <code>Row</code> or <code>Column</code> from the matrix, use the Trash can button. Keep in mind, deleting a <code>Row</code> or <code>Column</code> does <em>not</em> delete the corresponding object from the database, it only deletes its association with a particular Matrix. Deleting Rows and Columns likewise does not affect associated observations (to do so you must (un)score a row/column pair before it&#39;s deleted from a matrix).</li><li>To edit a <code>Row</code> or <code>Column</code>, for example to add a new character state to a descriptor, select <code>Edit</code> in its radial navigator.</li></ul><h3 id="adding-scores-to-a-row" tabindex="-1"><a class="header-anchor" href="#adding-scores-to-a-row"><span>Adding scores to a Row</span></a></h3><ul><li>In the <code>Edit Matrix</code> interface, select the scoring button <img src="https://sfg.taxonworks.org/s/p404ho" width="20px"> from the <code>Row</code> you want to score.</li><li>There you&#39;ll see the list of descriptors and for each descriptor its possible states. You can simply check applicable states or add values for numeric descriptors - all changes are saved automatically.</li></ul><figure><img src="https://sfg.taxonworks.org/s/xuxl3f" alt="" title=""><figcaption class="figure-left">Scoring a row of a matrix by selecting character states for the descriptors of the matrix</figcaption></figure><ul><li>Scores can also be cloned from another object in the matrix using the <code>Clone and copy</code> button. After cloning, only a few changes may be required to separate one OTU or Collection Object from another. It is recommended to select the most similar object for cloning.</li><li>The <code>Destroy all observations in row</code> button can be use to clear all scores from that row.</li><li>The <code>Matrix Row Coder</code> interface can also be accessed from the <code>Browse OTU</code> or <code>Browse Nomenclature</code> tasks. In either of those tasks, select the <code>Observation matrices</code> sector of the <code>OTU radial</code> button; from there you can select a matrix to automatically add this OTU to (if not added previously), and proceed directly to scoring mode.</li></ul><h3 id="adding-scores-to-a-column" tabindex="-1"><a class="header-anchor" href="#adding-scores-to-a-column"><span>Adding scores to a Column</span></a></h3><ul><li>In the <code>Edit Matrix</code> interface, select the scoring button <img src="https://sfg.taxonworks.org/s/p404ho" width="20px"> from the <code>Column</code> you want to score.</li><li>You will see the list of OTUs and Collection Object <code>Rows</code> from your matrix. Check applicable states or add values for numeric descriptors - all changes are saved automatically.</li><li>On the top of the form, there are options to <code>Destroy all observations</code>, <code>Populate column</code> (which assigns one particular character state to every row) and additional aids to scoring your column.</li></ul><figure><img src="https://sfg.taxonworks.org/s/1pn0ti" alt="" title=""><figcaption class="figure-left">Scoring a column of a matrix by selecting character states for the rows of the matrix</figcaption></figure><h3 id="using-a-matrix" tabindex="-1"><a class="header-anchor" href="#using-a-matrix"><span>Using a Matrix</span></a></h3><ul><li>From the <code>Observation Matrix Hub</code> task, select <code>View observation matrix</code> from the radial navigator of the matrix. In view mode, you can preview the entire matrix or download the matrix in <code>TNT</code>, <code>nexus</code>, <code>NeXML</code>, or <code>CSV</code> formats, to be used, for example, in phylogenetic reconstruction or to be uploaded into a Lucid builder. See the top of this page for an image of an observation matrix being viewed.</li><li>Once the matrix is completed, it can be used for species determination as an <code>Interactive Key</code>; that task is accessible from any radial navigator for the matrix, see below for details.</li><li>Before using the matrix for identification, it is recommended to validate the matrix. To validate a matrix, switch to <code>Edit</code> mode and then select the checkbox <code>Validation</code>. Each row of the matrix will be evaluated to see if it has enough resolution to separate this particular row from all other rows in the same matrix. If a row does not pass validation, the matrix can still be used for interactive identification, but adding some additional descriptors may improve the resolution.</li></ul><h2 id="interactive-key-interface" tabindex="-1"><a class="header-anchor" href="#interactive-key-interface"><span>Interactive key interface</span></a></h2><ul><li>The <code>Interactive key</code> interface is divided into 4 frames: the top one with the title and options; the middle one with the list of descriptors; and two lower frames: the list of rows matching the selected character states, and eliminated rows which do not match the selected character states, to be populated once the identification is started.</li></ul><figure><img src="https://sfg.taxonworks.org/s/gmsfrh" alt="" title=""><figcaption class="figure-left">The <code>Interactive key</code> interface, ready for use</figcaption></figure><ul><li>By default the descriptors are sorted by their weight and separating power. Other options are available in the <code>Descriptor sorting</code> box in the top panel.</li><li>To start the identification, select a character state from any descriptor. It is possible to select more than one state in case of uncertanty: to do so, click on the descriptor <em>name</em> (not the dropdown list) and make multiple selections.</li><li>By default the rows which do not have scores for the selected descriptors are not eliminated from the list: it is assumed that any character state is possible. This behavior can be changed by checking the <code>Eliminate unknowns</code> checkbox in the settings on the top of the page, if it is needed, for example, for the matrix evaluation.</li><li>If a particular row in the matrix does not have a score matching a selected character state, this OTU or collection object is moved to the list of Eliminated rows. That behavior can be slightly changed by adjusting the <code>Error tolerance</code> in the top panel. A tolerance equal to &#39;1&#39; means that a single error in identification is permitted, the row will be eliminated only when 2 or more errors exist, etc.</li><li>To clear the interactive key, and start the identification from the beginning, use the <code>Reset</code> button.</li><li>If the matrix is large, it may be more convenient not to eliminate rows after selection of each character, since the wait time could be several seconds before you can select a next character. In that case you may want to check the <code>Refresh only taxa</code> option. When checked, the list of descriptors will not be updated until the <code>Refresh</code> button is clicked. <ul><li>After the refresh, the taxa not matching the search criteria are moved to the right, and the list of descriptors is also updated to bring more useful ones closer to the top of the list.</li></ul></li><li>Continue the determination process in this way until only one or a few rows are still present in the list of Remaining.</li><li>It is also possible to use the <code>Select</code> button in the <code>Remaining</code> section to reduce the list of rows to be identified from the very beginning. If the list of entities is reduced, the list of descriptors will also be optimized to identify the remaining entries.</li><li>To make the interactive key more functional, you can add depictions to each character state of each descriptor. To do this, go back to the <code>Edit Matrix</code> interface and choose <code>Edit</code> from the radial navigator of a descriptor. In <code>Edit</code> mode, you can use the radial annotator next to each character state to add a depiction. Depictions of character states are displayed when you click on the name of a descriptor in an <code>Interactive key</code>.</li><li>If your project is set up on Taxon Pages, your interactive keys will automatically show up on their OTU page. For example <a href="https://hoppers.speciesfile.org/interactive_keys/11" target="_blank" rel="noopener noreferrer">here</a> is a taxon pages interactive key to species of <em>Erythroneura</em>:</li></ul><figure><img src="https://sfg.taxonworks.org/s/oogpvh" alt="" title=""><figcaption class="figure-left">The interactive key interface on Taxon Pages</figcaption></figure><h2 id="the-distinguish-app" tabindex="-1"><a class="header-anchor" href="#the-distinguish-app"><span>The Distinguish app</span></a></h2><p>An observation matrix in TaxonWorks can be used as a multi-entry key within or outside of TaxonWorks using the app <a href="https://github.com/SpeciesFileGroup/distinguish" target="_blank" rel="noopener noreferrer">distinguish</a>. Internally this integration extends the role of a multi-entry key engine far beyond its use in diagnosing taxa. For example the interface can be used to:</p><ul><li>Evaluate progress in the construction of large complicated datasets</li><li>Create a single point of entry for adding annotations of all types that reflect workflows, scientific addenda, etc.</li><li>Navigate to and from other linked resources within TaxonWorks</li><li>Provide multi-language context for the underlying coding of data</li></ul><p>Features include:</p><ul><li>Ability to translate any Descriptor (= Character traditionally) into any number of languages</li><li>Qualitative weighting of descriptors</li><li>Prioritization of choices based on descriptor order, weighting, or next-best-algorithm</li><li>Toggling between ranks of terminals (only species, only genera, etc.)</li><li>Trivial re-use and reformulation of the key scope by adding or removing new descriptors or terminals</li><li>Rows from OTUs (taxa), CollectionObjects, or Extracts</li></ul><h2 id="image-matrices" tabindex="-1"><a class="header-anchor" href="#image-matrices"><span>Image matrices</span></a></h2><p><em>An Image matrix is a specialized matrix which has defined descriptors and rows, but does not have any scores. Instead of scores, images are added as a depiction to each cell of the table. An image matrix can be used as a pictorial key for taxon identification; the images from the matrix are also displayed in the Browse OTU page. So instead of adding images directly to each OTU, they could be added to the image matrix instead to get additional functionality.</em></p><ul><li>To work with an image matrix, create a new matrix as described above.</li><li>Add rows to the matrix, OTUs for example.</li><li>Add new descriptors to the matrix - all should be of type <code>Media</code>. When creating media descriptors, only give the descriptor a name (no states); we&#39;ll describe how to add images later below. <ul><li>Usually image matrices have fewer descriptors than a typical morphological matrix, and each descriptor will represent a standard view of the organism or a particular structure useful for determination of taxa. For example, &#39;dorsal view&#39;, &#39;lateral view&#39;, &#39;head&#39;, &#39;wing venation&#39;.</li></ul></li></ul><p>Once the rows and columns are defined, the most convenient way to work with an image matrix is to use the <code>Observation Matrix Dashbord</code> task. Think of the dashboard as a filter task specialized for working with matrices, both image and otherwise.</p><ul><li>In the <code>Observation Matrix Dashbord</code> task, first select a <code>Taxon name</code> as the parent taxon and then select the rank of the children taxa which you would like to see in your matrix using the <code>Select ranks</code> facet. For example, select a genus name, and then select species rank to see all the species-level taxa from that particular genus. Once selected, click the <code>Search</code> button.</li><li>You will see a table of OTUs with a summary including how many observations each has and how many depictions are added to a particular OTU in an image matrix (<code>otu observation_depictions</code>). Unselectable rows with green buttons are those taxon names without an OTU; check <code>Show taxon names with OTUs only</code> in the <code>Otu</code> facet and then <code>Search</code> again to not show those, and/or select <code>Linked to valid only</code> to only display valid names.</li></ul><figure><img src="https://sfg.taxonworks.org/s/dgrzi2" alt="" title=""><figcaption class="figure-left">The <code>Observation Matrix Dashboard</code> showing data on the species of a genus</figcaption></figure><ul><li>Use the <code>Open in a matrix</code> button next to one of the OTUs to add that OTU to a matrix - in this case select your image matrix from the list. Once the matrix is selected, you get redirected to an editing interface that looks like a table, where your media descriptors are the headers of the columns.</li><li>To add an image to a particular cell of the table, simly drag and drop it from a folder on your computer into that cell. Images can also be dragged from one cell to another.</li><li>Once images have been added, you can provide some metadata. Click on an image to add a caption. Click the <code>Radial Annotator</code> button to add a citation and/or attribution.</li><li>Adding images to multiple OTUs: in the <code>Observation matrices dashboard</code>, once you filter the list of OTUs, select a few of them and click the <code>Edit image matrix</code> button. The table for editing multiple OTUs will be opened. WARNING: do not select all OTUs for editing, since ALL of them will be added to the matrix as separate Rows.</li></ul><figure><img src="https://sfg.taxonworks.org/s/m0ow1m" alt="" title=""><figcaption class="figure-left">An Image Matrix in edit mode</figcaption></figure><ul><li><p>You can select OTUs from the dashboard task in the same way you did above and use the <code>Add to matrix</code> button to add rows to any matrix, whether an image matrix or not.</p></li><li><p>The image matrix for an OTU can be accessed by selecting it from the <code>Observation matrix</code> slice of any <code>OTU Radial</code> button.</p></li><li><p>If your project is set up on Taxon Pages, your image matrices will automatically show up on their OTU page. For example <a href="https://hoppers.speciesfile.org/image_matrices/334" target="_blank" rel="noopener noreferrer">here</a> is an image matrix for &#39;American <em>Stenacronus</em>&#39;&#39;:</p></li></ul><figure><img src="https://sfg.taxonworks.org/s/van992" alt="" title=""><figcaption class="figure-left">The image matrix interface on Taxon Pages</figcaption></figure><h3 id="viewing-image-matrix-images-for-a-set-of-species" tabindex="-1"><a class="header-anchor" href="#viewing-image-matrix-images-for-a-set-of-species"><span>Viewing image matrix images for a set of species</span></a></h3><ul><li>In the <code>Observation Matrix Dashbord</code> task, select a parent taxon and the rank of the children taxa which you would like to see, for example, select a genus name, and select species rank to see all the species-level taxa from this particular genus. Once selected, click the <code>Search</code> button.</li><li>In the filtered results, <code>Select All</code> or a few OTUs using checkboxes before the names, and click the <code>View image matrix</code> button. A table will open to display all illustrations <em>from all media descriptors on any of the OTUs selected</em>, so that OTUs can be compared to each other.</li><li>In View mode, you can hide some rows by clicking the checkbox before each row, or you can quickly switch to Edit mode.</li></ul><h2 id="faq-related-to-how-to-form-descriptors-so-that-the-automatically-generated-descriptions-are-directly-usable-in-a-publication" tabindex="-1"><a class="header-anchor" href="#faq-related-to-how-to-form-descriptors-so-that-the-automatically-generated-descriptions-are-directly-usable-in-a-publication"><span>FAQ related to how to form descriptors so that the automatically generated descriptions are directly usable in a publication</span></a></h2><ul><li>Each descriptor and character state has additional fields, which are accessible using the <code>Show more</code> option in the Edit descriptor interface.</li><li>In the field <code>Description name</code>, provide alternative verbiage for a descriptor and each character state. For example, you have a descriptor &quot;Head, length to width ratio&quot; with a state &quot;2.2&quot;. That is how the character is displayed in the matrix. For <code>Description name</code>, alternative values could be provided, for example &quot;Head&quot; for descriptor and &quot;2.2x as long as wide&quot; for the state, so that the autogenerated description would have a sentence &quot;Head 2.2x as long as wide&quot;.</li><li>Two consecutive descriptors with the same <code>Description name</code> will be combined into a single sentence, for example &quot;Head 2.2x as long as wide; brown&quot;.</li></ul><h2 id="faq-related-to-how-to-add-alternative-descriptor-names-for-an-interactive-key" tabindex="-1"><a class="header-anchor" href="#faq-related-to-how-to-add-alternative-descriptor-names-for-an-interactive-key"><span>FAQ related to how to add alternative descriptor names for an interactive key</span></a></h2><ul><li>The procedure is the same as for the automatically generated description, the alternative values can be provided in the field <code>Key name</code>.</li></ul><h2 id="faq-related-to-how-to-organize-descriptors-in-the-interactive-key" tabindex="-1"><a class="header-anchor" href="#faq-related-to-how-to-organize-descriptors-in-the-interactive-key"><span>FAQ related to how to organize descriptors in the interactive key</span></a></h2><ul><li>By default, the descriptors are organized by their separating power, so that the descriptors that break the group more evenly are positioned closer to the top.</li><li>That order can be altered by adding a weight to each descriptor, using the <code>Show more</code> option in the Edit descriptor interface. Descriptors with higher weights are positioned closer to the top of the key.</li></ul>',60))])}const f=i(h,[["render",m]]),g=JSON.parse('{"path":"/guide/Manual/matrices.html","title":"Matrices","lang":"en-US","frontmatter":{"sidebarPosition":300},"git":{"updatedTime":1773764280000,"contributors":[{"name":"Debbie Paul","username":"","email":"dlpaul@illinois.edu","commits":2},{"name":"José Luis Pereira","username":"","email":"pepelui@outlook.com","commits":2},{"name":"Dmitry Dmitriev","username":"","email":"arboridia@gmail.com","commits":2},{"name":"Deborah Paul","username":"","email":"dlpaul@illinois.edu","commits":1},{"name":"Tommy McElrath","username":"","email":"monotomidae@gmail.com","commits":1},{"name":"Tom Klein","username":"","email":"twointofive@gmail.com","commits":2}],"changelog":[{"hash":"c0659429f734e51e49ad8c45e2c08afe06051a47","time":1773764280000,"email":"twointofive@gmail.com","author":"Tom Klein","message":"Update the matrices section, add images"},{"hash":"df361cc190ee1fc634155c4f524eaf41ba4bac70","time":1773697161000,"email":"twointofive@gmail.com","author":"Tom Klein","message":"Move <code>distinguish</code> reference to Matrix/interactive keys section"},{"hash":"081da63259a320615a7388e0d8c18c95eee0da87","time":1773108280000,"email":"monotomidae@gmail.com","author":"Tommy McElrath","message":"Update matrices.md"},{"hash":"2c9fa0344e8841325723ad18097b58ba2472a71b","time":1765817318000,"email":"proceps@users.noreply.github.com","author":"Dmitry Dmitriev","message":"Update matrices.md"},{"hash":"006e5256e0004c2e8cf18db07dbd36f7299b7802","time":1695167580000,"email":"dlpaul@illinois.edu","author":"Deborah Paul","message":"adding CO page, edit other pages"},{"hash":"8396ad50540543388a73a63dc0bc1553befe9168","time":1694210021000,"email":"arboridia@gmail.com","author":"Dmitry Dmitriev","message":"matrices"},{"hash":"ca8ca7ca38c26b00281a426c5fd0cc1ef98c2fc1","time":1660255897000,"email":"pepelui@outlook.com","author":"José Luis Pereira","message":"Update order"},{"hash":"06622727722a9a15ef2b35be8f7eaf3aac5e2950","time":1660255614000,"email":"pepelui@outlook.com","author":"José Luis Pereira","message":"Add order example"},{"hash":"9d236260af716e50b661341216862c9b3d3b14d4","time":1646954896000,"email":"dlpaul@illinois.edu","author":"Debbie Paul","message":"editing text"},{"hash":"83611d8123320cbd53d5a16dd744961cfd2f61c4","time":1646265820000,"email":"dlpaul@illinois.edu","author":"Debbie Paul","message":"bravely adding new pages"}]},"filePathRelative":"guide/Manual/matrices.md"}');export{f as comp,g as data};
